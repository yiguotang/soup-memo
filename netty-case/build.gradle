plugins {
    id 'java'
    id 'application'
}

group 'com.soup.memo'
version '1.0'

description = "netty-case"
archivesBaseName = "netty-case"

dependencies {
    compile(
            // netty
            'io.netty:netty-all:4.1.32.Final'
    )
}
