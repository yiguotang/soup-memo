plugins {
    id 'java'
    id 'application'
}

group 'com.soup.memo'
version '1.0'

description = "mq"
archivesBaseName = "mq"

dependencies {
    compile(
            // rabbitmq client
            "com.rabbitmq:amqp-client:5.8.0"
    )
}
